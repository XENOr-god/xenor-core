# Basic Run Example

This example demonstrates deterministic propagation in `xenor-core`.

## Example graph

Protocol -> NodeA -> NodeB
                  -> NodeC

## Run

```bash
cargo run
```

# Expected Output

```text
XENØr core engine starting
Propagation from NodeA -> ["NodeB", "NodeC"]
```
